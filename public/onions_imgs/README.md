This folder should contain your image files (pic_2.jpeg, img_1.avif, etc.).

To preserve exact image assets, move the `onions_imgs` folder from the project root into `react-app/public/` so the static HTML pages keep their original references.

Example (run in terminal):

mv "../onions_imgs" "./react-app/public/"

(Or move using your file explorer.)
